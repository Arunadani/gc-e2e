name: run-automation-test
on:  workflow_dispatch
jobs:
  firstJob:
    runs-on: ubuntu-latest
    steps:
      - name: greeting
        run: echo "Hello world"
      - name: goodbye
        run: echo "Done - bye"
